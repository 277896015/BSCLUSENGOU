{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip && pip install setuptools && pip install -r BSCLUSENGOU/requirements.txt"
  },
  "deploy": {
    "startCommand": "python BSCLUSENGOU/main.py"
  }
}
